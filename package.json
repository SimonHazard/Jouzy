{
	"name": "jouzy",
	"private": true,
	"workspaces": {
		"packages": [
			"apps/*",
			"packages/*"
		],
		"catalog": {
			"zod": "^4.4.3",
			"typescript": "^6.0.3",
			"@types/node": "^26.2.0",
			"lucide-react": "^1.27.0",
			"next-themes": "^0.4.6",
			"react": "^19.2.8",
			"react-dom": "^19.2.8",
			"sonner": "^2.0.7",
			"tailwindcss": "^4.3.3",
			"@types/react": "^19.2.17",
			"@types/react-dom": "^19.2.3"
		}
	},
	"type": "module",
	"scripts": {
		"lint": "biome check .",
		"paraglide:compile": "bun run --cwd apps/web paraglide:compile && bun run --cwd apps/admin paraglide:compile",
		"typecheck": "bun run paraglide:compile && bun run --cwd apps/web build && bun run --cwd apps/admin build && tsc -p packages/config/tsconfig.json --pretty false --noEmit && tsc -p packages/content/tsconfig.json --pretty false --noEmit && tsc -p packages/db/tsconfig.json --pretty false --noEmit && tsc -p packages/domain/tsconfig.json --pretty false --noEmit && tsc -p packages/ui/tsconfig.json --pretty false --noEmit && tsc -p apps/web/tsconfig.json --pretty false --noEmit && tsc -p apps/admin/tsconfig.json --pretty false --noEmit",
		"test": "bun run paraglide:compile && vitest run --config packages/config/vitest.config.ts",
		"build": "bun run --cwd apps/web build && bun run --cwd apps/admin build",
		"cf:typegen": "bun run --cwd apps/web cf:typegen && bun run --cwd apps/admin cf:typegen",
		"check": "bun run lint && bun run typecheck && bun run test && bun run build",
		"verify:env": "bun run scripts/verify-env-contract.ts"
	},
	"dependencies": {
		"zod": "catalog:"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.11",
		"@jouzy/config": "workspace:*",
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "^3.2.4",
		"@inlang/paraglide-js": "^2.25.0"
	},
	"packageManager": "bun@1.3.12"
}
